
heroku - Add Database Support in db config.

endless pagination
add text search on top
contact us
png extension
dropdown styling
sharing buttons
auto share on twitter and fb
google analytics
title
meta tag in each page
alt text on platform image
alt text on movie image
reduce file size of images
make responsive for mobile
add platform to search


jQuery ->
  if $('.pagination').length
    $(window).scroll ->
      url = $('.pagination .next_page').attr('href')
      if url && $(window).scrollTop() > $(document).height() - $(window).height() - 50
        $('.pagination').text("Loading More Movies...")
        $.getScript(url)
    $(window).scroll()
